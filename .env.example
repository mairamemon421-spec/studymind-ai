# Database Configuration
# Fallback to local SQLite if DATABASE_URL starts with sqlite
DATABASE_URL=sqlite+aiosqlite:///./study_planner.db

# Supabase API Keys (Optional, fallback to SQLite and local mock logic if empty)
SUPABASE_URL=
SUPABASE_KEY=

# Gemini API key for Multi-Agent AI Engine
GEMINI_API_KEY=

# JWT Secret for local auth
JWT_SECRET=super_secret_study_planner_key_123!
ACCESS_TOKEN_EXPIRE_MINUTES=1440
